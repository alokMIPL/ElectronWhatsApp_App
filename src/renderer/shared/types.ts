// Define a type for the slice state
export interface IMainState {
  value: number
}

export interface IChatHead {
  id: number,
  name: string,
  lastMessage: string,
  profileImage: string,
  phone: string,
}